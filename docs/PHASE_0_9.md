# Phase 0.9

The authoritative Phase 0.9 documentation is now in `PHASE_0_9_FINAL.md`.

This final package supersedes the earlier corrected build and includes the `.DS_Store` validation fix plus the additional database, resume, atomic-copy, and collision fixes documented there.
